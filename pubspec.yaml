name: models
description: "A new Flutter package project."
version: 0.0.1
publish_to: none
homepage:

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  equatable: 2.0.5
  flutter:
    sdk: flutter

dev_dependencies:
  lintingpackage:
      path: ../lintingpackage
